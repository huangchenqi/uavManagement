# 项目开发规则

## 代码结构规范

1. **文件命名**
   - C++类文件：使用小写命名，如 `uavmodeldao.h`、`uavmodeldao.cpp`
   - QML文件：使用大驼峰命名法，如 `DnyTable.qml`、`MainWindow.qml`
   - 实体类文件：使用大驼峰命名法，如 `UavModelEntity.h`
   - 配置文件：使用清晰的描述性命名，如 `configToDataBase.ini`

2. **目录结构**
   - 保持模块化组织，将相关功能文件放在同一目录下
   - 配置文件放在 `config/` 目录下
   - 数据库迁移文件放在 `migrations/` 目录下
   - 资源文件放在 `Resources/` 目录下

## 数据库交互规范

1. **DAO模式**
   - 所有数据库操作必须通过DAO（数据访问对象）类进行
   - DAO类必须继承 `QObject` 并使用 `Q_OBJECT` 宏
   - 暴露给QML的方法必须使用 `Q_INVOKABLE` 标记

2. **数据库连接**
   - 使用 `DatabaseConnection` 类管理数据库连接
   - 数据库配置信息存储在 `configToDataBase.ini` 文件中
   - 不得在代码中硬编码数据库连接信息

3. **ODB使用规范**
   - 实体类与数据库表必须一一对应
   - 确保所有实体类都有对应的 `-odb.hxx`、`-odb.cxx` 文件
   - 使用ODB预编译器生成数据库映射代码

## QML界面开发规范

1. **组件复用**
   - 使用自定义QML组件（如 `DnyTable.qml`）提高代码复用率
   - 公共UI元素应封装为单独的QML文件

2. **数据绑定**
   - 使用 `qmlRegisterType` 注册C++类到QML中
   - 使用JSON格式进行C++和QML之间的数据传输
   - 避免直接在QML中进行复杂的数据处理逻辑

3. **界面响应性**
   - 表格控件必须支持自适应列宽和动态行高
   - 确保UI在不同分辨率和窗口大小下正常显示
   - 使用异步操作处理耗时的数据库查询，避免UI阻塞

## 编码风格

1. **C++代码规范**
   - 使用智能指针管理动态分配的对象
   - 类成员变量以下划线结尾（如 `dbConn_`）
   - 公共方法使用驼峰命名法（首字母小写）
   - 使用Qt的信号槽机制进行对象间通信

2. **QML代码规范**
   - 组件ID使用小驼峰命名法
   - 属性名使用小驼峰命名法
   - 信号名使用小驼峰命名法
   - 合理使用锚点布局和RowLayout/ColumnLayout

3. **注释规范**
   - 所有公共方法必须有注释说明功能
   - 复杂的算法或逻辑必须有注释
   - 使用中文注释说明业务逻辑，英文注释说明技术实现

## 数据管理规范

1. **数据验证**
   - 在将数据写入数据库前，必须进行验证
   - 使用 `checkUavDataObject` 等方法验证数据完整性和正确性

2. **错误处理**
   - 使用统一的错误处理机制
   - 确保所有错误信息能够被记录和显示给用户
   - 数据库操作失败时，必须提供明确的错误信息

3. **版本控制**
   - 使用Git管理代码版本
   - 提交前确保代码可以成功编译
   - 每次提交必须有清晰的提交信息

## 性能优化规范

1. **查询优化**
   - 大型查询结果应分页返回
   - 避免在循环中执行数据库查询
   - 使用批量操作代替单条操作

2. **资源管理**
   - 确保所有资源（如数据库连接）正确释放
   - 大型图片或资源应考虑延迟加载
   - 使用恰当的缓存机制减少重复查询

## 安全规范

1. **数据安全**
   - 敏感数据（如数据库密码）不应硬编码在源代码中
   - 使用参数化查询防止SQL注入
   - 数据库用户应使用最小权限原则

2. **错误处理**
   - 不应向最终用户显示详细的技术错误信息
   - 所有异常必须被捕获和处理
   - 记录详细的错误日志以便调试 